# G File Studio v2.18.47

## 图元标准检查报告可读性升级
- HTML 报告从“只给数量”改为“检查概览 + 文件汇总 + 不符合标准明细”三层结构。
- 每个不符合项逐条显示：文件、RMU、RMU 框 ID、设备角色、XML 元素、元素 ID、设备名、旋转角度、关联 ConnectLine。
- 明确解释“为什么不符合”，包括 SMART/NORMAL 图元变体错误、当前 devref 与 ACTIVE 标准 devref 不一致、尺寸/锚点几何差异。
- 报告同时显示“当前值”和“标准要求”，几何差异会给出当前尺寸/位置与标准尺寸/建议位置。
- 文件汇总表增加“主要不符合原因”，不再只显示一个不符合数量。
- 新增 `symbol-standard-check-details.csv`，每个不符合元素一行，便于筛选、整改和交付。
- `Nonstandard Symbols` 改为按不符合元素唯一计数，避免同一个元素同时存在 devref 与几何差异时重复计数。
- 图元标准检查继续严格只读，不修改源 G；同类 OLD→NEW 图元版本升级仍只在“基础处理”模块执行。

## 验证
- 全量自动化测试：369 passed，2 skipped。

---

# G File Studio v2.18.46

- 将“图元标准检查”移动到“ID 检查与修复”下方。
- 图元标准检查改为严格只读：仅检查、告警、生成 CSV/HTML 报告，不再提供“检查并升级”，也不生成 final G。
- 图元标准检查的 workspace 输出目录移动到 G 文件输入模块下方。
- 检测到图元类型/变体、devref 或几何与 ACTIVE 标准不一致时弹出明确告警，并在日志中列出当前/期望 devref。
- “基础处理 → 通用图元升级”更名为“同类图元版本升级”，明确只处理同一设备语义/同一 XML 类型的 OLD → NEW 版本变化；SMART/NORMAL 用错归类为图元类型/变体使用错误，不再称为版本升级。

# G File Studio v2.18.45

## 手动 OLD → NEW 图元配对
- 通用图元升级不再要求旧/新图元文件名一致；同名仅作为自动配对的优先规则。
- “配对选中”改为更明确的“手动配对…”。选中一条旧图元后即可打开配对窗口，也可以不选行直接打开。
- 手动配对窗口分别提供 OLD 与 NEW 下拉框，并实时显示双方 XML 元素类型、主体 ID、w/h、AlignCenter、pin 数量与坐标，确认对应关系后再建立映射。
- 手动映射允许文件名、主体 ID 不同；仍强制校验 XML 元素类型和端口拓扑，结构不兼容时禁止配对。
- 一个新标准图元可以被多个旧图元手动映射，支持多个历史别名统一升级到同一个新标准图元。
- 解除配对或删除旧映射时，仅当该 NEW 已不再被其他 OLD 使用时才重新列为“未配对新图元”。
- v2.18.43 的 90°/270° 非正方形图元中心旋转与连接线锚点保护、v2.18.44 的通用图元标准模板全部保留。

## 验证
- 全量自动化测试：367 passed，2 skipped。

---

# G File Studio v2.18.44

## 通用图元标准模板
- “图元标准检查”的前 6 行继续保留 SMART/NORMAL RMU 的 LBS、Circuit Breaker、接地刀闸系统规则，不改变既有业务判断。
- 新增“添加设备图元”“添加扫描到的未映射图元”“删除选中自定义项”，允许用户继续维护任意设备图元标准。
- 自定义规则支持范围 `ANY / SMART / NORMAL`、设备角色、XML 元素类型、标准 devref，以及 `devref / XML元素 / p_NameString / key_name` 四种业务元素匹配方式。
- 检查/升级引擎已支持这些自定义图元；内置 RMU 规则优先执行，已由内置规则处理的设备不会被自定义规则重复修改；多个自定义规则同时命中同一元素时安全跳过并告警。

## G 图元属性目录
- 扫描标准主 G 时，自动记录每个 devref 的 XML 元素类型、主体 ID、w/h、旋转样本、p_NameString/key_name 示例及出现次数。
- 如果标准目录中同时包含原始图元定义 G，直接解析 `AlignCenter`、`pin(cx,cy)`、pin id 和图元主体属性。
- 原始图元定义 G 即使声明 GBK/GB18030，也使用已有安全解码器解析，不再因为 ElementTree 的多字节编码限制而漏扫。
- 图元属性直接显示在“标准定义”表格中，便于确认“业务元素 → 标准图元”的对应关系。

## 通用几何与安全
- 保存的图元几何模板不再只限于 CBreakerDis / 接地刀闸；任意带 devref 且能确认 ConnectLine 电气锚点的设备均可建立几何标准。
- 原始图元定义 G 可直接生成 0/90/180/270 四个方向的 pin 几何模板，并继续使用 v2.18.43 的 zenon 中心旋转算法处理非正方形图元。
- 自定义图元有安全几何模板时保持 ConnectLine 电气锚点绝对位置不变；无法可靠学习几何时只替换 devref，不擅自移动连接线。
- 修复仅几何发生变化但 devref 未变化时未写出结果的问题。

## 兼容性
- Profile JSON 升级为 version 4，新增 `custom_symbols` 与 `symbol_catalog`；旧版 Profile 可直接加载。
- 自定义规则和几何变化会参与版本历史；图元属性目录本身的样本统计更新不会无意义地产生新标准版本。
- v2.18.43 的批量 OLD/NEW 自动配对、90/270° 非正方形图元中心旋转和横平竖直连接线保护完整保留。

## 验证
- 全量自动化测试：364 passed，2 skipped。
