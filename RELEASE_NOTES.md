# G File Studio v2.18.52

## 本次修改

- 修复吉达馈线批处理在 NORMAL Circuit Breaker 图元标准发生变化时只更新 devref、没有同步新图元 pin 几何的问题。
- 图元标准 Profile 会从已扫描的标准 `*.icn.g` 属性目录中自动提取 `w/h` 和 `pin(cx,cy)`，生成 0/90/180/270 四个方向的有效几何模板；已有 Profile 加载后也可自动补全。
- 如果当前 G 中只有目标图元的某一个正确旋转方向，也可以反推原始 pin 并生成其他旋转方向模板，避免“标准里有新图元，但当前方向识别不到”。
- 吉达批处理纠正图元时仍保持 ConnectLine 电气端点绝对位置不变，仅调整图元 `x/y/w/h`。
- 针对用户提供的 30815 Q1 实例验证：`Circuit_Breaker_NON-SMART` → `Circuit_Breaker_NO-SMART` 后，Q1 从 `(1442,2541,28×28)` 调整为 `(1436,2539,30×30)`，两端连接点仍为 `(1454,2545)` 与 `(1454,2565)`。
- 继续使用单一 `VALIDATION.md`，不生成分散验证文件。

## 基线

- 直接基于 v2.18.51。
- 不改变其他吉达固定流程、ID、keyid、node_area、ConnectLine 拓扑及既有业务规则。
