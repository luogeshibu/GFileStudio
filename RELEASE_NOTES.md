# G File Studio 2.18.205

本版仅调整“服务器图元同步管理”的图元信息表格展示，不改变业务处理逻辑。

- 7 个可见字段按实际内容计算自然宽度，并在空间充足时自动铺满整个表格。
- 窗口较窄时保留完整字段宽度，通过表格自己的横向滚动条查看，不再把文件名、Pins、状态或分类标记压缩到看不全。
- 表格 viewport 尺寸变化后自动重新分配剩余空间，行为参考“ID 检查与修复”表格。
- 200 条本地缓存只在完成填充后测量一次可见文本；后续窗口缩放只复用缓存宽度，不重新扫描所有行。
