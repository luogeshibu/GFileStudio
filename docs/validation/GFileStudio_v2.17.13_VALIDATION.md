# G File Studio v2.17.13 验证报告

## 本次修改

仅在 v2.17.12 基础上新增馈线合并的“合并主母线为一条”功能。

- 默认关闭，不影响原有馈线合并行为。
- 开启时，仅处理最顶部、非零长度、水平的 `<Bus>`；`<BusDis>` 不参与。
- 保留第一条主母线 ID，删除其他顶部主母线。
- 保留母线范围扩展到所有馈线主母线的最左端至最右端。
- 所有被删除 Bus 的 `node_area` / `link` / `p_FatherObjId` 引用改接到保留 Bus。
- 保留 Bus 汇总原全部主母线的连接分组。
- 输出后仍调用用户确认的 ID 规则模板进行强制检查/规范。

## 真实合并文件检查

对用户提供的 `MERGED-20260811_213029.sln.pic.g` 执行母线合并算法：

- 识别顶部水平主母线：46 条
- 保留：1 条（ID `30000001`）
- 删除：45 条
- 合并母线 X 范围：317 ~ 75087
- 合并母线 Y：379
- 汇总主母线连接分组：46 个
- 被删除的主母线 ID 在已知引用属性中残留：0
- 非顶部 Bus / 零长度 Bus / BusDis：保持不变

## 自动化测试

`pytest -q`：150 passed。
