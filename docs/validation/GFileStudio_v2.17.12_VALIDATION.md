# G File Studio v2.17.12 验证报告

## 本次变更

1. 馈线图合并新增默认单线图宽度参数，默认 1000。
2. ID 规则删除立即持久化；删除内置规则后刷新与重启均不会自动恢复。
3. ID 扫描新增模板覆盖统计和未覆盖类型列表。
4. 可推断的新类型会自动带入 XML 类型、ID 前缀、总位数，逐条由用户确认后加入规则模板。

## 馈线最小宽度验证

构造两张实际宽度均为 100 的馈线，设置：

- 默认单线图宽度 = 1000
- 相邻馈线间隔 = 300

第二张馈线起始 X 与第一张馈线起始 X 相差 1300，验证“1000 占用宽度 + 300 用户间隔”独立生效。

## ID 删除验证

- 删除默认 `ConnectLine` 规则后立即从模板消失。
- 重新加载规则服务后仍保持删除状态。
- 用户再次新增并确认 `ConnectLine=34 / 8位` 后可正常恢复。

## 自动化测试

`148 passed`。
