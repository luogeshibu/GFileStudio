# G File Studio v2.17.33 验证报告

日期：2026-08-12

## 本版范围

1. 异常小尺寸图元检测：扫描/处理报告改为固定文件名并覆盖同类上一份报告；“打开本次 HTML 报告”简化为“打开报告”；扫描与执行按钮增加报告提示。
2. ID 检查与修复：扫描/修复报告分别覆盖同类上一份报告；按钮增加报告提示；扫描发现新元素类型后逐条人工确认加入模板的既有逻辑保持不变。
3. App 页面重构：新增独立“环网柜处理”页面，位于“ID 检查与修复”之后、“基础处理”之前；原基础处理中的“环网柜图元处理”和“环网柜名称与柜型识别”迁移至该页，底层仍调用原 RMU 处理引擎。
4. 基础处理不再展示环网柜相关控件，其他基础处理能力不变。

## 报告覆盖文件名

- 异常小尺寸扫描：`small-element-scan-report.csv/html`
- 异常小尺寸处理：`small-element-process-report.csv/html`
- ID 扫描：`id-scan-report.csv/html`
- ID 强制修复：`id-repair-report.csv/html`

重复执行同一类型任务时直接覆盖以上对应文件，不再累积时间戳报告。

## 自动化验证

- `pytest -q`：198 passed
- 报告覆盖行为专项脚本：通过（第二次写入复用同一路径，目录不增加同类历史报告）
- Python `compileall`：通过

## 环境说明

当前容器未安装 PySide6，因此未在容器中启动完整 Qt GUI 做运行时截图验证；UI 源码与自动化源代码测试均已通过。Windows 实机打包/显示仍建议在项目现有 `.venv` 中执行一次 smoke test。
