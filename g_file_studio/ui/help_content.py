from __future__ import annotations

APP_HELP: dict[str, tuple[str, str]] = {
    "pipeline": (
        "一键处理帮助",
        """
<h3>用途</h3>
<p>按选中的步骤依次执行：基础处理 → G 文件合并 → 添加图框。每个阶段也可以在左侧对应页面单独运行。</p>
<h3>合并输入要求</h3>
<ul>
<li>文件名可以任意命名，但必须以 <b>.sln.pic.g</b> 结尾。</li>
<li>程序不会检查文件是否属于同一个站点。</li>
<li>扫描文件后可用上移、下移、置顶、置底自由定义顺序；第一行作为合并基准。</li>
<li>输入文件不能已经包含最外层框架、左上标题块或右下签字栏。</li>
</ul>
<h3>目录</h3>
<ul>
<li><b>原始输入目录</b>：存放需要处理的 G 文件。</li>
<li><b>中间工作目录</b>：保存基础处理和合并的中间结果。</li>
<li><b>最终输出目录</b>：保存最终交付文件。</li>
</ul>
<p>启用“运行前清理中间目录”后，只会清理工作目录中的阶段目录，不会删除原始输入文件。</p>
""",
    ),
    "basic": (
        "基础处理帮助",
        """
<h3>规则化处理</h3>
<p>每一项处理规则都可以单独启用或关闭。基础处理页和一键处理页使用同一个规则编辑组件。</p>
<p><b>自动选项：</b>点击“扫描元素与属性”后，程序会读取输入目录中的 G 文件，在元素标签下拉框中列出直属 Layer 的直接子元素；选择标签后，属性名下拉框会列出该标签实际出现的属性。下拉框仍可手动输入。</p>
<p><b>处理范围：</b>删除和属性替换只检查 G 根节点直属 Layer 的直接子元素，不修改 G、Theme、Layer 外内容，也不递归修改图元内部子元素。</p>
<h3>当前通用规则</h3>
<ul>
<li>按元素标签、属性名和旧值替换属性内容。</li>
<li>按元素标签、属性名和属性值删除整个匹配元素。</li>
</ul>
<h3>删除匹配元素</h3>
<p>三项必须全部精确匹配。删除的是整个 XML 元素及其子元素，不是只清空一个属性。任何 Layer 直属图元都可以通过该规则删除，例如 ConnectLine、Disconnector 等，不再设置单独的专用删除规则。程序会同步清理 link、node_area 和 p_FatherObjId 中指向已删除真实图元的引用。</p>
<p>所有输出均写入输出目录，原始文件不会被修改。</p>
""",
    ),
    "merge": (
        "G 文件合并帮助",
        """
<h3>文件要求</h3>
<ul>
<li>文件名任意，但后缀必须是 <b>.sln.pic.g</b>。</li>
<li>App 中可由用户自由调整合并顺序；未手动调整时默认按文件名自然排序。</li>
<li>不解析站点、馈线号，也不判断文件是否属于同一个站。</li>
<li><b>输入文件不能包含外框架图</b>。请使用尚未添加最外层边框、标题块和签字栏的原始图。</li>
</ul>
<h3>垂直对齐</h3>
<ul>
<li>只识别标签名严格等于 <b>&lt;Bus&gt;</b> 的有效非零长度水平母线，<b>&lt;BusDis&gt;</b> 不会被当作 Bus。</li>
<li>存在有效水平 &lt;Bus&gt; 时，选择线坐标 Y 最小的最上方 Bus 作为对齐基准。</li>
<li>没有有效水平 &lt;Bus&gt; 时，使用该文件所有位置坐标中的最小 Y（最高图元）作为对齐基准，并与第一张基准图的统一 Y 对齐。</li>
</ul>
<h3>其他处理</h3>
<ul>
<li>清理负坐标图元。</li>
<li>保持相邻图形的实际水平间隔。</li>
<li>处理重复 ID，并同步更新 link、node_area、p_FatherObjId。</li>
<li>统一坐标取整并重新计算画布尺寸。</li>
</ul>
<p>点击“扫描 / 检查”读取文件。使用上移、下移、置顶、置底调整顺序；第一行文件完整作为合并基准。表格同时显示对齐方式、原始基准 Y 和外框检查结果。</p>
""",
    ),
    "frame": (
        "添加图框帮助",
        """
<h3>用途</h3>
<p>把固定 SLD 图框模板添加到已经合并完成的 .g 文件中。</p>
<h3>自动处理</h3>
<ul>
<li>根据目标画布大小调整外边框。</li>
<li>左上角标题默认取输入文件名中 .sln.pic.g 前面的部分。</li>
<li>将 Draw、Approve、Issue 姓名和日期写入右下角信息栏。</li>
<li>为模板图元重新分配唯一 ID，避免与目标文件冲突。</li>
</ul>
<p>JSON 配置可用于保存和复用人员与日期信息。</p>
""",
    ),
    "help": (
        "帮助中心",
        """
<h3>推荐使用方式</h3>
<ol>
<li>首次使用先在“基础处理”“G 文件合并”“添加图框”页面确认各项参数。</li>
<li>需要完整流程时使用“一键处理”。</li>
<li>只需某一步时，直接进入对应页面运行。</li>
</ol>
<h3>重要限制</h3>
<p>参与合并的输入必须以 .sln.pic.g 结尾，并且不能包含外框架图。外框应在合并完成后通过“添加图框”阶段统一加入。合并顺序以 App 表格从上到下为准。</p>
<h3>安全原则</h3>
<p>程序不会直接修改输入文件。处理结果写入指定输出目录，并在写出后重新解析 XML 进行合法性检查。</p>
<h3>快捷帮助</h3>
<p>界面中的圆形问号按钮、字段提示和鼠标悬停说明都可以查看具体帮助。按 F1 可打开本帮助中心。</p>
""",
    ),
}

FIELD_HELP: dict[str, str] = {
    "input_dir": "选择包含待处理 G 文件的目录。基础处理会忽略非 .g 文件。",
    "merge_input_dir": "合并目录只允许包含以 .sln.pic.g 结尾的 G 文件；输入图不能带外框架。扫描后可在表格中自由调整顺序。",
    "output_dir": "处理结果写入此目录。输入文件不会被覆盖。",
    "work_dir": "一键流程的中间目录，用于保存各阶段临时结果。",
    "template": "选择 SLD 图框模板 .g 文件。模板应包含唯一直属 Layer。",
    "feeder_gap": "相邻两张输入图真实坐标边界之间的水平距离，默认 300。",
    "merge_margin": "合并完成后图形内容距离画布四边的距离，默认 300。该整数框只支持直接输入，鼠标滚轮不会改变数值。",
    "frame_margin": "最外层图框距离目标 G 画布边缘的距离，默认 50。该整数框只支持直接输入，鼠标滚轮不会改变数值。",
    "title": "留空时自动使用输入文件名去掉 .sln.pic.g 后的内容。",
    "output_name": "合并输出统一使用 .sln.pic.g 后缀；留空时生成 MERGED.sln.pic.g。",
    "output_suffix": "可在 .sln.pic.g 前添加后缀；留空则保持原文件名。",
    "draw": "填写 Draw 姓名；日期默认是当前日期，可通过右侧日历按钮修改，鼠标滚轮不会改变日期。",
    "approve": "填写 Approve 姓名；日期默认是当前日期，可通过右侧日历按钮修改，鼠标滚轮不会改变日期。",
    "issue": "填写 Issue 姓名；日期默认是当前日期，可通过右侧日历按钮修改，鼠标滚轮不会改变日期。",
    "clean_work": "执行一键流程前清理本次使用的中间阶段目录，避免旧文件混入。",
}
