# G File Studio 设计说明（v1.5.0）

## 1. 总体架构

```text
PySide6 UI
    ↓
Processor 业务层
    ↓
Engine 核心算法层
    ↓
ElementTree / 文件系统
```

UI 负责参数、文件顺序、帮助、日志和进度；Processor 负责业务编排；Engine 负责 XML、坐标、ID、引用、合并布局和图框算法。

## 2. 基础处理规则

基础处理页和一键处理页共同使用：

```text
g_file_studio/ui/widgets/basic_rules_editor.py
```

当前规则：

1. 替换元素属性值；
2. 删除匹配元素。

两条规则都默认关闭，输入框默认为空，仅提供占位提示。

### 2.1 替换规则

精确匹配：

```text
元素标签 == replace_target_tag
元素[属性名] == replace_old_value
```

匹配后：

```text
元素[属性名] = replace_new_value
```

### 2.2 删除规则

精确匹配：

```text
元素标签 == delete_target_tag
元素[属性名] == delete_target_value
```

匹配后删除整个元素子树，并收集该元素及后代 ID。

### 2.3 处理范围

只处理 G 根节点直属 Layer 的直接子元素：

```text
G
├── Theme                  不处理
└── Layer
    ├── 直接子元素          处理
    └── Group
        └── 嵌套子元素      不处理
```

删除后仅在当前 Layer 内清理 `link`、`node_area`、`p_FatherObjId`。

## 3. 合并顺序

`FileOrderEditor` 扫描输入目录，默认采用自然排序。用户可使用：

- 置顶；
- 上移；
- 下移；
- 置底；
- 恢复自然排序。

表格从上到下就是实际合并顺序，第一行完整作为基准文件。

Engine 会验证用户顺序：

- 必须包含目录中的全部 `.sln.pic.g` 文件；
- 不能遗漏；
- 不能重复；
- 不能包含不存在的文件。

## 4. 垂直对齐算法

### 4.1 Bus 识别

只接受本地标签名严格等于：

```xml
<Bus ... />
```

不会把下列元素识别为 Bus：

```xml
<BusDis ... />
```

Bus 必须满足：

- 可以取得 `x1/y1/x2/y2`，或从 `d` 取得至少两个点；
- 水平；
- 长度大于 0。

零长度 Bus 不作为对齐母线。

### 4.2 顶部 Bus

一个文件存在多个有效水平 `<Bus>` 时：

```text
第一优先：Y 最小
第二优先：同一 Y 时长度最长
```

线的实际 Y 取 `y1/y2`；水平母线二者相等。

### 4.3 无 Bus 回退

文件没有有效水平 `<Bus>` 时，计算 Layer 所有位置坐标的最小 Y：

- `y`；
- `y1/y2`；
- `cy`；
- `mergey`；
- `d` 中所有 Y。

该最小 Y 代表最高图元位置。

### 4.4 统一目标线

用户顺序中的第一张文件决定统一目标 Y：

```text
第一张有 Bus    → 第一张顶部 Bus Y
第一张无 Bus    → 第一张最高图元 Y
```

后续每张文件：

```text
本图有 Bus      → 本图顶部 Bus 移动到统一目标 Y
本图无 Bus      → 本图最高图元移动到统一目标 Y
```

所以“与上一张或前面已有 Bus 对齐”在实现上等价为：所有输入图都与已合并图的同一公共目标 Y 对齐。

## 5. 整数输入控件

`IntegerInput` 基于 `QSpinBox` 保留整数校验，但：

- 隐藏上下按钮；
- 忽略鼠标滚轮；
- 忽略 Up、Down、PageUp、PageDown；
- 只允许用户直接编辑数字。

该控件用于：

- 馈线间隔；
- 合并四边距；
- 图框四边距。

## 6. 日期输入控件

`OptionalDateEdit` 基于 `QDateEdit`：

- 启用日历弹窗；
- 支持空日期；
- 输出 `yyyy-MM-dd`；
- 忽略鼠标滚轮。

`PersonEditor` 将姓名输入和日期选择组合，供 Draw、Approve、Issue 共用。

## 7. 外框限制

参与合并的输入不能包含外框架图。检测逻辑包括：

- 接近画布边缘并覆盖大部分画布的四条线；
- 接近画布边缘并覆盖大部分画布的大矩形。

正确流程：

```text
基础处理 → 馈线合并 → 添加图框
```

## 8. 文件安全

- 不直接修改原输入文件；
- 基础处理写入临时文件，重新解析成功后原子替换输出；
- 合并和图框输出均执行 XML 重新解析；
- 一键流程可清理工作阶段目录，避免旧结果混入。

## 9. 后续扩展建议

- 动态增加多条替换/删除规则；
- 保存和载入规则配置；
- G 文件校验报告；
- 设备关联检查；
- 图形预览；
- 项目文件和最近项目；
- 安装包和数字签名。
