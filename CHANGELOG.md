# CHANGELOG

## 1.8.0

- 基础处理页面新增“单个 G 文件 / G 文件目录”输入方式。
- 添加图框页面新增“单个 G 文件 / G 文件目录”输入方式。
- 单文件模式只处理用户选择的一个文件，并将结果写入指定输出目录。
- 目录模式批量处理目录第一层中的全部 `.g` 文件。
- 新增通用 `InputMode`，基础处理、添加图框和一键处理共用同一输入类型。
- 新增 `discover_g_inputs()`，统一 Processor 层的文件发现与输入校验。
- 基础规则的元素标签和属性扫描会自动跟随当前单文件或目录输入。
- 一键处理适配新的基础处理和图框输入模型，内部阶段仍统一使用目录模式。
- 新增基础处理单文件测试和添加图框单文件测试。
- 自动测试：25 passed。

## 1.7.0

- 一键处理支持单个 `.sln.pic.g` 文件或 G 文件目录。
- 一键处理页面移除中间工作目录，只保留原始输入与最终输出。
- 新增 `TempWorkspaceService`，使用 AppData 缓存保存中间阶段文件。
- App 启动、新任务开始、正常关闭和下次启动时自动清理中间缓存。
- 新增内置模板/客户自定义模板两种模式。
- 内置模板通过 `templates.json` 进行 ID、名称和版本管理。
- 新增“导出内置模板”功能。
- 客户模板按四边距调整外框和锚定组件位置，但不修改任何文字或签字内容。
- 外框识别不再依赖模板原始边距必须为 50。
- 内置模板仍支持标题、Draw、Approve、Issue 与日期修改。
- 基础规则的元素和属性扫描支持单个输入文件。
- 新增模板几何、客户模板内容保护、一键单文件和缓存清理测试。
- 自动测试：23 passed。
