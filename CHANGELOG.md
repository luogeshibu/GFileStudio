# CHANGELOG

## 2.0.0

- 使用用户选定的绿色 G File Studio 图标。
- 新增 `resources/icons/app.ico` 和 `app.png`。
- PyInstaller 打包增加 `--icon`，EXE 不再使用默认 PyInstaller 图标。
- App 启动时设置 QApplication 和 MainWindow 图标。
- Windows 下设置独立 AppUserModelID，改善任务栏图标识别。
- 新增 `UserSettingsService`，使用 QSettings 持久保存最近目录。
- 单文件输入和目录输入分别保存自己的最近目录。
- 基础处理、G 文件合并、添加图框和一键处理分别保存输出目录。
- 客户自定义模板、内置模板导出和图框 JSON 配置分别保存最近目录。
- 上次目录不存在时弹窗提示用户重新选择，并自动回退到有效目录。
- 每次执行前新增输入文件、输入目录、输出目录和模板路径校验。
- App 启动时自动创建默认 workspace 目录。
- 新增程序图标和最近路径静态测试。
- 自动测试：33 passed。

## 1.9.0

- 所有业务下拉框在收起状态下禁用鼠标滚轮切换。
- 下拉列表展开后仍可使用滚轮浏览。
- 可编辑下拉框仍支持手动输入。

## 1.8.0

- 基础处理和添加图框均支持单文件或目录输入。

## 1.7.0

- 一键处理支持单文件或目录。
- 中间目录改由 App 缓存自动管理和清理。
- 增加内置模板与客户自定义模板模式。
