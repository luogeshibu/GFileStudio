# Changelog

## 1.5.0

- “替换元素属性值”规则默认关闭。
- 替换规则的标签、属性名、旧值、新值默认清空，仅显示占位提示。
- 新增 `IntegerInput`：整数参数只允许直接输入，隐藏步进按钮，并禁止鼠标滚轮、上下方向键和 PageUp/PageDown 改值。
- 合并间隔、合并四边距、图框四边距统一使用无滚轮整数输入。
- Draw、Approve、Issue 日期改为带日历弹窗的可空日期选择框。
- 日期选择框禁止鼠标滚轮改变日期。
- Bus 识别规则明确为严格标签 `<Bus>`，不把 `<BusDis>` 当作 Bus。
- 对齐母线必须是有效、非零长度、水平 `<Bus>`；多个 Bus 时选择 Y 最小者，同高时选择最长者。
- 无有效水平 `<Bus>` 时使用 Layer 所有位置坐标中的最小 Y（最高图元）对齐。
- 新增 BusDis 排除、零长度 Bus 排除、顶部 Bus 选择、无 Bus 回退和替换默认关闭测试。

## 1.4.0

- 删除专用“删除 ConnectLine w=137”基础规则。
- 基础处理只保留“替换元素属性值”和“删除匹配元素”两条通用规则。
- 任意直属 Layer 图元都可通过“元素标签 + 属性名 + 属性值”精确匹配删除。
- 基础处理模型、Processor、UI、帮助文档和测试同步精简。
- 删除和替换继续严格限定为 G 根节点直属 Layer 的直接子元素。
- G、Theme、Layer 本身、Layer 外内容以及图元内部嵌套子元素保持不变。
- 保留用户自定义合并顺序、无母线最高图元对齐、外框拒绝、ID 与引用处理等全部 v1.3.0 功能。

## 1.3.0

- 合并页面和一键处理页面新增用户自定义文件顺序。
- 新增“扫描 / 检查、恢复自然排序、置顶、上移、下移、置底”操作。
- 用户顺序中的第一行文件作为合并基准。
- 引擎校验顺序列表必须完整覆盖全部 `.sln.pic.g` 文件，且不能重复。
- 删除匹配元素和属性替换仅作用于 G 根节点直属 Layer 的直接子元素。
- G、Theme、Layer 外内容以及图元内部嵌套子元素保持不变。

## 1.2.0

- 为所有复选框增加边框、悬停状态、选中背景和白色勾号。
- 新增“删除匹配元素”基础规则。
- 基础处理页和一键处理页共用 `BasicRulesEditor`。
- 合并输入不再解析站点和馈线号。
- 合并文件名只要求以 `.sln.pic.g` 结尾。
- 无水平 Bus 时使用最高图元 Y 作为对齐基准。
- 新增外框架图检测和拒绝处理。
