# G File Studio v2.17.11 验证报告

## 本次修改范围

仅修改“基础处理 -> 环网柜名称与柜型识别”的柜名匹配逻辑；柜型、SMART、ID、馈线合并、图形边距、图框等其他逻辑保持不变。

## 柜名规则

1. Text 柜名搜索严格限制在用户勾选的方向，未勾选方向不参与搜索或兜底。
2. 同一方向继续使用全局一对一匹配，避免相邻环网柜重复使用同一个 Text。
3. 单候选直接使用；同一近邻组多候选时绿色优先。
4. 如果指定方向内无法分配独立柜名 Text，不跨方向寻找；仅允许读取该柜内部唯一 BusDis.key_name 元数据作为回退，例如 `30864_BUS -> 30864`。
5. 元数据回退结果标记为“中”置信度并写入告警，方便审计。

## JED-NTH-ABH.sln.pic.g 实测

仅勾选“上方”：

- 有效环网柜：340
- 柜名成功：340
- 柜型成功：340
- 未命名：0

v2.17.10 未识别的三个柜在 v2.17.11 中：

- rect 2000333 -> 30864（BusDis key_name: 30864_BUS）
- rect 2000362 -> 30833（BusDis key_name: 30833_BUS）
- rect 2000429 -> 30859（BusDis key_name: 30859_BUS）

整个过程中没有搜索“下方/左侧/右侧”文字。

## 自动化测试

`146 passed`
