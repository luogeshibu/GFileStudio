# G File Studio v2.17.0 验证结果

## 新功能

基础处理新增复选框：

```text
将馈线名称移动到母线上方
```

配置字段：

```text
BasicSettings.move_feeder_titles_above_bus
```

默认关闭；勾选后随“开始基础处理”执行。

## 模型字段独立性

`feeder_title_engine.py` 不读取 `key_name` 或 `keyid`。测试同时覆盖字段为空的 Bus/Text，结果与字段不存在时一致。

## 真实文件回归

### busbartextmove.sln.pic.g

- 有效水平 Bus：3 条；
- 自动分组：2 组，其中一组为双母线；
- 移动 Text：2 个；
- `ABS-36` ID `8000000`：`(501, 226) → (432, 160)`；
- `ABS-36` ID `8000012`：`(909, 443) → (807, 357)`；
- 数值 `255` 未修改。

### JED-NTH-ABH-22.sln.pic.g

- 有效水平 Bus：1 条；
- 自动分组：1 组；
- `ABH-22` ID `8000012`：`(953, 421) → (868, 339)`；
- `2000.00`、`UPDATED_MEASURMENT`、设备编号、SMART/Y/Q 标签和远端标题均未修改。

## 最小修改范围

逐图元属性对比确认：两份真实文件合计只有 3 个目标 `<Text>` 发生变化，并且变化属性严格为：

```text
x
y
```

Bus、连接线、设备、ID、文字内容、字体、字号、颜色以及模型关联属性均保持不变。

## 安全性

- 候选不唯一时跳过；
- 同一个 Text 不会分配给多个母线组；
- 目标位置与其他 Text 冲突时逐级增加上方间距；
- 无法避让时跳过；
- 重复执行第二次不再修改，保持幂等。

## 自动化测试

```text
116 passed
```
