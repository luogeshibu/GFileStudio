# G File Studio v2.16.1 验证结果

## 自动化测试

```text
112 passed
```

## MODE-ZZZ 真实文件回归

输入：`MODE-ZZZ.sln.pic(1).g`

连接点修复结果：

- 连接线：1486 个；
- 母线：102 个；
- 可连接设备：744 个；
- 新增缺失引用：1227 处；
- 修改已有引用端口：0 处；
- 删除已有引用：0 处；
- 连接线坐标修改：0 个；
- 原文件已有 `node_area/link` 三元组丢失：0 个；
- 第二次重复执行变更：0 个。

已专项核对 v2.16.0 出现端口交换/合并的设备：

```text
100005611  CBreaker
101005612  Disconnector
100005621  CBreaker
101005622  Disconnector
101005625  Disconnector
101005626  Disconnector
```

以上设备的原 `node_area` 在 v2.16.1 输出中逐字保持不变。

## 小型连接文件回归

`connection-problem.g`：

- 新增引用 24 处；
- Q1、Q2 两侧连接补齐；
- 四个接地刀闸连接补齐；
- 已有引用修改 0 处、删除 0 处。

## 半像素吸附回归

`connection-alignment-problem.g`：

- 保守对齐 41 个验证通过的半像素设备；
- 只修改设备 X；
- `ConnectLine/FeedLine` 路径及坐标修改 0 个；
- 原有引用修改 0 处、删除 0 处。

## 安全边界

- 原连接三元组被改变时，文件处理直接失败，不输出结果；
- 多候选、端口冲突、无法建模的设备自动跳过；
- 整数坐标设备和超过 0.5 像素的偏差不自动移动。
